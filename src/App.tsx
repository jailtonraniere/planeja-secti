import{useEffect,useMemo,useState}from'react';import{BarChart3,CheckCircle2,ChevronRight,Download,Home,Printer,Save,Search,TriangleAlert,WifiOff}from'lucide-react';import{areas,axes,indicatorCatalog,objectives,strategy}from'./data';import{load,save,type Store}from'./storage';import type{Participant,Project,Status}from'./types';import{normalizeDisplay,validate}from'./validation';
type Page='home'|'identify'|'projects'|'edit'|'review'|'dashboard';
const steps=['Enquadramento','Iniciativas e meta','Resultados anuais','Indicador e métrica'];
export function App(){const[store,setStore]=useState<Store>(load);const[page,setPage]=useState<Page>('home');const[selected,setSelected]=useState<string>('');const[step,setStep]=useState(0);const[saved,setSaved]=useState(false);const[query,setQuery]=useState('');const[statusFilter,setStatusFilter]=useState('Todos');const project=store.projects.find(p=>p.id===selected);
 useEffect(()=>{const t=setTimeout(()=>{save(store);setSaved(true);setTimeout(()=>setSaved(false),1400)},350);return()=>clearTimeout(t)},[store]);
 const update=(patch:Partial<Project>)=>{if(!project)return;setStore(s=>({...s,projects:s.projects.map(p=>p.id===project.id?{...p,...patch,updatedAt:new Date().toISOString(),updatedBy:s.participant?.name||'Participante'}:p),history:[...s.history,{at:new Date().toISOString(),author:s.participant?.name||'Participante',project:project.name,summary:'Rascunho atualizado'}]}))};
 const goProject=(id:string)=>{setSelected(id);setStep(0);setPage('edit')};
 const exportCsv=()=>{const rows=store.projects.map(p=>[p.name,p.executive,p.axis,p.objective,p.area,p.goal,p.actual2025,p.target2026,p.target2027,p.target2028,p.unit,p.indicator,p.metric,p.owner,p.status]);const csv=[['Projeto','Executiva','Eixo','Objetivo','Área','Meta','2025','2026','2027','2028','Unidade','Indicador','Métrica','Responsável','Status'],...rows].map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(';')).join('\n');download('planeja-secti.csv','\ufeff'+csv,'text/csv')};
 return <div className="app"><header><button className="brand" onClick={()=>setPage('home')}><span>PS</span><div><b>Planeja SECTI</b><small>Oficina 2025–2028</small></div></button><nav><button onClick={()=>setPage('projects')}><Search/>Projetos</button><button onClick={()=>setPage('dashboard')}><BarChart3/>Painel</button></nav><div className="save">{!navigator.onLine?<><WifiOff/>Offline</>:saved?<><CheckCircle2/>Salvo</>:<><Save/>Autosave</>}</div></header>
 <main>{page==='home'&&<HomePage start={()=>setPage(store.participant?'projects':'identify')} dashboard={()=>setPage('dashboard')}/>} {page==='identify'&&<Identify initial={store.participant} done={p=>{setStore(s=>({...s,participant:p}));setPage('projects')}}/>} {page==='projects'&&<Projects projects={store.projects} query={query} setQuery={setQuery} open={goProject} add={name=>{const id=crypto.randomUUID();const p:Project={id,name,executive:store.participant?.executive||'',axis:'',objective:'',area:'',secondaryArea:'',initiatives:[],goal:'',actual2025:'',target2026:'',target2027:'',target2028:'',unit:'',indicator:'',metric:'',frequency:'',owner:store.participant?.name||'',notes:'',status:'Em preenchimento',provenance:'Proposta para discussão',updatedAt:new Date().toISOString(),updatedBy:store.participant?.name||''};setStore(s=>({...s,projects:[...s.projects,p]}));goProject(id)}}/>} {page==='edit'&&project&&<Editor p={project} step={step} setStep={setStep} update={update} review={()=>setPage('review')}/>} {page==='review'&&project&&<Review p={project} back={()=>setPage('edit')} update={update}/>} {page==='dashboard'&&<Dashboard projects={store.projects} filter={statusFilter} setFilter={setStatusFilter} open={goProject} csv={exportCsv}/>}</main></div>}
function HomePage({start,dashboard}:{start:()=>void;dashboard:()=>void}){return <section className="hero"><div className="eyebrow">OFICINA DE PLANEJAMENTO ESTRATÉGICO</div><h1>Decisões claras.<br/><em>Resultados acompanháveis.</em></h1><p>Espaço coletivo para estruturar programas e projetos da SECTI no ciclo 2025–2028.</p><div className="hero-actions"><button className="primary" onClick={start}>Iniciar planejamento <ChevronRight/></button><button className="secondary" onClick={dashboard}>Acompanhar oficina</button></div><div className="principles"><article><small>MISSÃO</small><b>Base oficial</b><p>{strategy.mission}</p></article><article><small>VISÃO</small><b>Base oficial</b><p>{strategy.vision}</p></article><article><small>VALORES</small><b>Base oficial</b><p>{strategy.values}</p></article></div></section>}
function Identify({initial,done}:{initial?:Participant;done:(p:Participant)=>void}){const[p,setP]=useState(initial||{name:'',executive:'',role:''});return <section className="panel narrow"><div className="kicker">Antes de começar</div><h2>Como você participa desta oficina?</h2><p>Esta identificação acompanha o histórico das alterações.</p><label>Seu nome *<input value={p.name} onChange={e=>setP({...p,name:e.target.value})}/></label><label>Executiva *<input value={p.executive} onChange={e=>setP({...p,executive:e.target.value})} placeholder="Nome da executiva"/></label><label>Função ou área<input value={p.role} onChange={e=>setP({...p,role:e.target.value})}/></label><button className="primary" disabled={!p.name||!p.executive} onClick={()=>done(p)}>Continuar <ChevronRight/></button></section>}
function Projects({projects,query,setQuery,open,add}:{projects:Project[];query:string;setQuery:(s:string)=>void;open:(s:string)=>void;add:(s:string)=>void}){const list=projects.filter(p=>p.name.toLowerCase().includes(query.toLowerCase()));return <section className="content"><div className="title-row"><div><div className="kicker">Portfólio institucional</div><h2>Escolha um projeto</h2></div><button className="secondary" onClick={()=>{const n=prompt('Nome do novo programa ou projeto');if(n)add(n)}}>+ Novo projeto</button></div><div className="search"><Search/><input placeholder="Pesquisar projetos" value={query} onChange={e=>setQuery(e.target.value)}/></div><div className="project-grid">{list.map(p=><button className="project-card" key={p.id} onClick={()=>open(p.id)}><div><span className={'badge '+statusClass(p.provenance)}>{p.provenance}</span><h3>{normalizeDisplay(p.name)}</h3><p>{p.executive} · {p.area||'Área pendente'}</p>{p.divergence&&<small><TriangleAlert/> Divergência registrada</small>}</div><ChevronRight/></button>)}</div><p className="notice">SUAS Inteligente foi excluído do portfólio institucional. Voucher 99 não integra o portfólio estratégico.</p></section>}
function Editor({p,step,setStep,update,review}:{p:Project;step:number;setStep:(n:number)=>void;update:(x:Partial<Project>)=>void;review:()=>void}){return <section className="content"><div className="title-row"><div><div className="kicker">{p.provenance}</div><h2>{p.name}</h2></div><span className="badge amber">{p.status}</span></div><div className="progress"><i style={{width:`${(step+1)*25}%`}}/></div><div className="steps">{steps.map((s,i)=><button className={i===step?'active':''} onClick={()=>setStep(i)} key={s}><span>{i+1}</span>{s}</button>)}</div><div className="panel form">{step===0&&<Strategic p={p} update={update}/>} {step===1&&<><h3>Iniciativas e meta estratégica</h3><p>Iniciativa é o que será feito. Meta é o resultado que se deseja alcançar.</p><label>Iniciativas (uma por linha)<textarea value={p.initiatives.join('\n')} onChange={e=>update({initiatives:e.target.value.split('\n')})}/></label><label>Meta estratégica *<textarea value={p.goal} onChange={e=>update({goal:e.target.value})}/></label></>} {step===2&&<Annual p={p} update={update}/>} {step===3&&<Indicator p={p} update={update}/>}<div className="form-actions"><button className="secondary" disabled={step===0} onClick={()=>setStep(step-1)}>Voltar</button>{step<3?<button className="primary" onClick={()=>setStep(step+1)}>Continuar <ChevronRight/></button>:<button className="primary" onClick={review}>Revisar ficha <ChevronRight/></button>}</div></div></section>}
function Strategic({p,update}:{p:Project;update:(x:Partial<Project>)=>void}){return <><h3>Enquadramento estratégico</h3><p>Escolha uma classificação principal. Uma segunda área pode ser registrada como observação.</p><label>Eixo principal *<select value={p.axis} onChange={e=>update({axis:e.target.value,objective:'',area:''})}><option value="">Selecione</option>{axes.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Objetivo estratégico *<select value={p.objective} disabled={!p.axis} onChange={e=>update({objective:e.target.value,area:''})}><option value="">Selecione</option>{(objectives[p.axis]||[]).map(x=><option key={x}>{x}</option>)}</select></label><label>Área temática principal *<select value={p.area} disabled={!p.objective} onChange={e=>update({area:e.target.value})}><option value="">Selecione</option>{(areas[p.objective]||[]).map(x=><option key={x}>{x}</option>)}</select></label><label>Área secundária (opcional)<input value={p.secondaryArea} onChange={e=>update({secondaryArea:e.target.value})}/></label>{p.divergence&&<div className="warning"><TriangleAlert/><div><b>Divergência a validar</b><p>{p.divergence}</p></div></div>}</>}
function Annual({p,update}:{p:Project;update:(x:Partial<Project>)=>void}){return <><h3>Resultados anuais</h3><p>Todos os anos usam a mesma unidade. Campo vazio continua como pendente.</p><label>Unidade de medida *<input value={p.unit} onChange={e=>update({unit:e.target.value})} placeholder="Ex.: pessoas, %, serviços"/></label><div className="annual">{[['Realizado 2025','actual2025'],['Meta 2026','target2026'],['Meta 2027','target2027'],['Meta 2028','target2028']].map(([l,k])=><label key={k}>{l}<input type="number" value={String(p[k as keyof Project])} onChange={e=>update({[k]:e.target.value})}/></label>)}</div></>}
function Indicator({p,update}:{p:Project;update:(x:Partial<Project>)=>void}){const suggestions=indicatorCatalog.filter(i=>!p.executive||p.executive==='Pendente'||i.executive.toLowerCase()===p.executive.toLowerCase()).slice(0,40);return <><h3>Indicador estratégico principal</h3><p>Use um único indicador principal. O catálogo oficial oferece sugestões; você pode criar outro.</p><label>Indicador *<input list="indicator-catalog" value={p.indicator} onChange={e=>{const i=indicatorCatalog.find(x=>x.name===e.target.value);update(i?{indicator:i.name,metric:i.metric,unit:i.unit,frequency:i.frequency,owner:p.owner||i.owner}:{indicator:e.target.value})}} placeholder="Pesquisar entre 150 indicadores"/><datalist id="indicator-catalog">{suggestions.map(i=><option key={`${i.sourceRow}-${i.name}`} value={i.name}>{i.subject} · {i.executive}</option>)}</datalist></label><label>Fórmula ou descrição da métrica *<textarea value={p.metric} onChange={e=>update({metric:e.target.value})} placeholder="Explique exatamente como calcular"/></label><div className="two"><label>Frequência<select value={p.frequency} onChange={e=>update({frequency:e.target.value})}><option value="">Selecione</option><option>Mensal</option><option>Trimestral</option><option>Semestral</option><option>Anual</option></select></label><label>Responsável pelo preenchimento *<input value={p.owner} onChange={e=>update({owner:e.target.value})}/></label></div><label>Observações<textarea value={p.notes} onChange={e=>update({notes:e.target.value})}/></label></>}
function Review({p,back,update}:{p:Project;back:()=>void;update:(x:Partial<Project>)=>void}){const errors=validate(p);return <section className="content"><div className="title-row"><div><div className="kicker">Revisão final</div><h2>{p.name}</h2></div><button className="secondary" onClick={()=>window.print()}><Printer/> Imprimir ficha</button></div>{errors.length>0&&<div className="warning"><TriangleAlert/><div><b>{errors.length} pendência(s)</b><ul>{errors.map(e=><li key={e}>{e}</li>)}</ul></div></div>}<div className="sheet"><h3>Ficha consolidada</h3><dl><dt>Enquadramento</dt><dd>{p.axis||'Pendente'} → {p.objective||'Pendente'} → {p.area||'Pendente'}</dd><dt>Meta estratégica</dt><dd>{p.goal||'Pendente'}</dd><dt>Resultados</dt><dd>2025: {p.actual2025||'Pendente'} · 2026: {p.target2026||'Pendente'} · 2027: {p.target2027||'Pendente'} · 2028: {p.target2028||'Pendente'} {p.unit}</dd><dt>Indicador</dt><dd>{p.indicator||'Pendente'}</dd><dt>Métrica</dt><dd>{p.metric||'Pendente'}</dd><dt>Responsável</dt><dd>{p.owner||'Pendente'}</dd></dl></div><div className="form-actions"><button className="secondary" onClick={back}>Voltar e editar</button><button className="primary" disabled={errors.length>0} onClick={()=>update({status:'Pronto para validação'})}>Marcar pronto para validação</button></div></section>}
function Dashboard({projects,filter,setFilter,open,csv}:{projects:Project[];filter:string;setFilter:(s:string)=>void;open:(s:string)=>void;csv:()=>void}){const counts=useMemo(()=>Object.fromEntries(['Não iniciado','Em preenchimento','Pendente','Pronto para validação','Validado'].map(s=>[s,projects.filter(p=>p.status===s).length])),[projects]);const shown=filter==='Todos'?projects:projects.filter(p=>p.status===filter);return <section className="content"><div className="title-row"><div><div className="kicker">Visão do facilitador</div><h2>Painel da oficina</h2></div><button className="primary" onClick={csv}><Download/> Exportar CSV</button></div><div className="stats"><article><b>{projects.length}</b><span>Total</span></article>{Object.entries(counts).map(([s,n])=><article key={s}><b>{n}</b><span>{s}</span></article>)}</div><div className="dashboard-grid"><div className="panel"><h3>Principais lacunas</h3><p>{projects.filter(p=>validate(p).length).length} projetos com campos pendentes</p><div className="bar"><i style={{width:`${projects.length?100-projects.filter(p=>validate(p).length).length/projects.length*100:0}%`}}/></div><h3>Divergências</h3><p>{projects.filter(p=>p.divergence).length} aguardando decisão</p></div><div className="panel"><div className="table-head"><h3>Projetos</h3><select value={filter} onChange={e=>setFilter(e.target.value)}><option>Todos</option>{Object.keys(counts).map(s=><option key={s}>{s}</option>)}</select></div>{shown.map(p=><button className="row" onClick={()=>open(p.id)} key={p.id}><span><b>{p.name}</b><small>{p.executive}</small></span><em>{p.status}</em></button>)}</div></div></section>}
function download(name:string,text:string,type:string){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();URL.revokeObjectURL(a.href)}function statusClass(p:string){return p.includes('Divergência')?'red':p.includes('Importada')?'blue':'amber'}
